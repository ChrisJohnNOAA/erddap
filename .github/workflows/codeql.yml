name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '35 15 * * 2'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          # Keep actions to monitor your workflow files for security flaws
          - language: actions
            build-mode: none
          # Switch Java to manual build mode for maximum taint-tracking accuracy
          - language: java-kotlin
            build-mode: manual

    steps:
    - name: Checkout repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4.2.2
      with:
        persist-credentials: false

    # Set up the Java runtime specifically for the manual compilation step
    - name: Set up JDK
      if: matrix.build-mode == 'manual'
      uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v4.7.0
      with:
        java-version: '25' # Adjust to match ERDDAP's required target JDK
        distribution: 'temurin'
        cache: 'maven'

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        config-file: ./.github/codeql/codeql-config.yml

    # Intercept the Java compilation process to map the Abstract Syntax Tree
    - name: Run manual build steps
      if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        mvn clean compile -DskipTests

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@ff2f1c621b7f889edc0d3c761ac2e6a3f8cdb0dd # v4.37.7
      with:
        category: "/language:${{matrix.language}}"
